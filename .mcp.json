{
  "mcpServers": {
    "contatos": {
      "type": "stdio",
      "command": "python",
      "args": ["mcp/server.py"],
      "env": {
        "API_URL": "http://localhost:8000",
        "MCP_USERNAME": "admin@exemplo.com",
        "MCP_PASSWORD": "sua_senha_aqui"
      }
    }
  }
}
